import { makeStyles } from '@mui/styles';

const useStyle = makeStyles({
  root: {
    'background-color': '#b4d69c !important', // !important pour changer la prio du css
  },
});
export default useStyle;
